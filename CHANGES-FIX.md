# پچ رفع باگ ارسال پیام ویزارد «شروع پروژه» + رفع مشکلات امنیتی

تاریخ: 2026-09-01 — بر پایه‌ی برنچ `bugfix` (کامیت e3242af)

فایل‌های این پکیج را با همان مسیرها روی پروژه کپی کنید (نیازی به مایگریشن جدید نیست).

## ۱. باگ اصلی — پیام ویزارد به پنل ادمین نمی‌رسید (app/assets/js/contact.js)
در مرج آخر، کل بلوک ارسال به سرور از `contact.js` حذف شده بود؛ دکمه‌ی «ارسال درخواست پروژه»
فقط صفحه‌ی موفقیت را نشان می‌داد و هیچ درخواستی به `POST /{lang}/inquiry` ارسال نمی‌شد،
بنابراین هیچ پیامی در «پیام‌های دریافتی» ادمین ثبت نمی‌شد.

رفع شد:
- تابع `submitInquiry()` برگردانده شد: ارسال با `fetch` + `FormData` شامل توکن CSRF
  (هم فیلد `csrf_token` هم هدر `X-CSRF-TOKEN`)، فیلد honeypot، دسته‌بندی‌ها، اطلاعات تماس،
  توضیحات، یادداشت و فایل‌های پیوست.
- صفحه‌ی موفقیت فقط وقتی نشان داده می‌شود که سرور واقعاً `{"ok":true}` برگرداند؛
  در غیر این صورت پیام خطا + کد وضعیت نمایش داده می‌شود (`showSubmitError`).
- `kind` برابر `project` ارسال می‌شود (ویزارد سفارش پروژه است).

## ۲. رفع XSS در contact.js
- `populateReview()` ورودی کاربر (نام، شرکت، ایمیل، تلفن) را با `innerHTML` تزریق می‌کرد؛
  حالا کاملاً با DOM API و `textContent` ساخته می‌شود.
- لیست فایل‌های انتخاب‌شده هم نام فایل را با `innerHTML` می‌نوشت؛ به `textContent` تغییر کرد.

## ۳. اعتبارسنجی سمت کلاینت
- ایمیل: به‌جای بررسی صرفِ وجود `@`، همان الگوی سختگیرانه‌ی سمت سرور بررسی می‌شود
  (بن‌بست در مرحله‌ی آخر ایجاد نمی‌شود).
- پیوست‌ها: حداکثر ۳ فایل، هر کدام ≤ ۲MB و فقط pdf/doc/docx/png/jpg/jpeg/zip —
  همان سیاست سرور، ولی هنگام انتخاب فایل با پیام فارسی/انگلیسی هشدار می‌دهد.

## ۴. app/controllers/InquiryController.php — مقاوم‌سازی
- گارد اسکیمای دیتابیس قبل از هر کوئری روی `messages`: اگر جدول مایگریت نشده باشد،
  به‌جای Exception و صفحه‌ی ۵۰۰، پاسخ تمیز JSON با کد `server` برمی‌گردد و در audit-log ثبت می‌شود.
- ذخیره‌سازی داخل try/catch: خطای ذخیره هرگز به‌صورت «موفقیت الکی» به کاربر نشان داده نمی‌شود.

## ۵. app/views/layouts/footer.php
- کاراکتر اضافه‌ی «چ» قبل از `<?php` حذف شد (در تمام صفحات عمومی قبل از DOCTYPE چاپ می‌شد
  و خروجی HTML را نامعتبر می‌کرد).

## ۶. app/views/contact.php و app/core/lang.php
- کلید `submitError` به دیکشنری JS صفحه برگردانده شد و ترجمه‌ی `contact.err.submit`
  (فارسی و انگلیسی) به `lang.php` اضافه شد.

## تست‌های انجام‌شده (End-to-End روی MariaDB واقعی)
- ارسال ویزارد → `{"ok":true}` → رکورد در جدول `messages` → نمایش در `/admin/messages`
  همراه با نشان «خوانده‌نشده» ✔
- بدون توکن CSRF → 419 ✔
- honeypot پر شده → پاسخ ok ولی هیچ رکوردی ثبت نمی‌شود ✔
- پیوست PDF → ذخیره و فقط از طریق روت احرازهویت‌شده‌ی ادمین قابل دانلود ✔
- فایل php → 415 رد می‌شود ✔
- دسترسی عمومی به `/uploads/attachments/…` → 404 ✔
- `php -l` روی همه‌ی فایل‌های PHP و `node --check` روی contact.js ✔
